
/**
 * Toma una cadena nombre y un número num (0 ó 1) y devuelva "Hello" + nombre si num es 1,
 * en caso contrario devuelva "Bye" + nombre.
 * @param name - a string
 * @param num - "Hello" or "Bye", depending on the name
 */

function helloBye() {
  // Inicio código
  // Fin código
}

module.exports = helloBye;
