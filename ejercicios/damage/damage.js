/**
 * Dado un tipo de pokémon, el tipo del pokémon al que ataca, la estadística de
 * ataque del pokémon atacante y la estadística de defensa del pokémon defensor,
 * calcula el daño que hace el ataque.
 * @param yourType - String, the type of your character
 * @param opponentType - the type of the opponent's Pokémon.
 * @param attack - the attack of the attacking Pokemon
 * @param defense - the defense of the opponent
 */
function calculateDamage(yourType, opponentType, attack, defense){
  // inicio código
  // fin código
}

module.exports = calculateDamage;
