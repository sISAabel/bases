/**
 * Un número feliz es un número que da un 1 al sumar repetidamente el cuadrado de sus dígitos.
 * Si dicho proceso da como resultado un ciclo interminable de números (o repeticiones), se dice
 * que el número es un número infeliz. Crea una función que acepte un número y determine si el
 * número es un número feliz o no. Devuelve true si es así, false en caso contrario.
 *
 * @see toString()
 * @see parseInt()
 * @see push()
 * @see includes()
 * @see .length
 *
 * @param {*} inputArray
 */

function isHappy(n) {
  // Inicio código
  // Fin código
}
module.exports = isHappy;
