/**
 * Escribe una función que haga lo siguiente para los valores dados: sumar, restar,
 * dividir y multiplicar.
 *
 * Debes tener en cuenta que las variables (incluido los números), están definidos
 * como strings utiliza algunas condiciones y aplicar la operación aritmética.
 *
 * Notas
 * El resultado debe ser un valor entero.
 * Si la operación da como resultado Infinity, entonces devuelve "undefined" (por ejemplo en división por cero).
 *
 * The function should return the result of the operation
 * @param num1 - The first number to be used in the calculation.
 * @param num2 - The second number to be used in the calculation.
 * @param op - The operation to perform.
 * @see https://developer.mozilla.org/es/docs/Web/JavaScript/Reference/Global_Objects/Infinity
 * @see https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/parseInt
 */
function calculator (num1, num2, op) {
  // Inicio código
  // Fin código
}

module.exports = calculator
