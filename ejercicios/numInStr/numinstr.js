/**
 * Crea una función que toma un array de cadenas y devuelve un array con sólo
 * las cadenas que tienen números. Si no hay cadenas que contengan números,
 * devuelve una matriz vacía.
 *
 * @param {*} inputArray
 */
function numInStr(inputArray) {
  // Inicio código
  // Fin código
}
module.exports = numInStr;
