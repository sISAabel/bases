/**
 * Un vehículo necesita 10 veces más combustible que la distancia que recorre.
 * Sin embargo, siempre debe llevar un mínimo de 100 de combustible antes de partir.
 *
 * Crea una función que calcule la cantidad de combustible que necesita, dada la distancia.
 * Ejemplos
 *  calculateFuel(15) ➞ 150
 *  calculateFuel(23.5) ➞ 235
 *  calculateFuel(3) ➞ 100
 *
 * Nota
 * La distancia será un número mayor que cero.
 * Devuelve 100 si el combustible calculado resulta ser menor que 100.
 */

function calculateFuel (value) {
  // Inicio código
  // Fin código
}
module.exports = calculateFuel
