/**
 * Se te dará un array de bebidas, siendo cada bebida un objeto con dos propiedades:
 * nombre (name) y precio en céntimos (price).
 * Crea una función que tenga como argumento el array de bebidas y que devuelva los
 * objetos bebidas ordenados por precio de forma ascendente (de menos caro a más caro)
 */

// Inicio código
function sort ()
{
}
// Fin código

module.exports = sort
